# 更新日志

本文件记录 DiagTools 各版本的重要变更，格式遵循
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [1.6.0] - 2026-09-01

### 新增
- **刷写报告自动生成**: 刷写终态（成功/失败/取消）自动落盘HTML报告到
  reports/，含步骤状态明细与起止时间，报告中心自动归类识别
- **报告中心导出PDF**: HTML报告→PDF 零依赖转换（QTextDocument+QPrinter，
  支持中文），无需引入reportlab等第三方库
- **车辆总览扫描异常黄色警示**: 扫描异常时拓扑全部节点黄色标记，
  区别于"扫描完成但离线"的灰色态；通信异常卡同步计数，新扫描结果自动清除
- **示例资源**: resources/a2l/sample_demo.a2l（3测量+2标定）、
  resources/flash_config/sample_flash_config.json（刷写配置导入演示）
- 工具栏连接/断开改为CANoe风格启动/停止互补按钮: 未连接时连接亮黄可点/
  断开置灰，连接后反转；连接面板按钮同步同一套配色规则；
  日志面板折叠收缩为紧凑细条（原折叠残留170px空白）

### 修复
- **A2L解析器**: 剥离 `/* */` 与 `//` 注释（原来注释文本导致PROJECT名误识别）；
  地址提取优先 ECU_ADDRESS 字段（真实A2L的IF_DATA场景），回退固定位置参数
- 帮助菜单版本硬编码: "检查更新"/"关于"改为读取 src.__version__
  （原硬编码V2.1.0）；用户手册补全标定/测试中心等已实现功能的描述（原文案仍标"预留"）
- QSS特异性: 连接/断开按钮的置灰禁用态需显式 `#btn_*:disabled` 规则，
  否则ID选择器压制通用禁用样式、禁用态仍渲染彩色看似可点
- 多帧响应被整体超时误杀、连接时按地址自动识别ECU等修复（随v1.5.0后续批次）

### 变更
- **项目树「资源」节点落地**: 双击Flash跳转刷写中心、双击A2L打开导入对话框
  （原来只显示"P1/P2预留"提示）；删除无调用点的 _reserved() 死函数
- 快捷键帮助更新: Ctrl+1~8（8个一级导航页）

## [1.5.0] - 2026-09-01

### 新增
- **多 ECU 热切换**: 已连接状态下点击项目树直接切换 ECU，重定向 UDS
  寻址并重新自检，无需断开重连；连接时按通信地址自动识别 ECU，
  避免"界面显示 BCM、实际在跟 DASH 说话"的错位
- **在线状态真实化**: 项目树图标/状态卡由连通性自检结果驱动，
  区分"总线已连接"与"ECU 可达"；自检失败提示附带总线 TX/RX 计数证据
- **CAN 帧级日志**: 所有接口的 TX/RX 帧自动落盘到 logs/comm/
  （含 CAN ID），只发不收类问题可直接从日志判读
- **tools/bus_listen.py**: 独立于主程序的总线监听诊断工具
  （纯 python-can，OBD 诊断专用 CAN 场景探测）
- 工具栏按钮联动: 连接后"连接"禁用/"断开"与"全车扫描"可用；
  右上角全局连接状态标签（其它页面无 Online 指示的唯一常驻反馈）

### 修复
- **多帧响应被整体超时误杀**: transport 层接收改按 ISO 15765-2
  N_Cr 语义逐帧重置超时——真实 ECU 下"自检通过但 DTC/DID 全超时"的根因
- **PCAN 概率性重连失败 (0x40)**: 断开时先停使用者再关总线、
  失败时驱动句柄清理重试（递增延时）、断开前等待后台线程退出
- **DID 面板点击无反应**: 同步 UDS 调用改后台任务队列（UdsWorker），
  真实总线等 ECU 响应期间界面不再冻结
- **虚拟 ECU 多帧请求**: 首帧回流控帧、连续帧按序重组（写 DID >7 字节请求）
- **高 DPI 小屏内容裁切**: 工作区包 QScrollArea + DPI 切换延迟二次校正
  （150% 缩放逻辑可用区 1280×752 下中间 330px 不再被截断）
- **序列面板**: 点击"请求HEX"焦点跳到名称列（释放事件强制定位 bug）、
  手抖误触行拖拽排序、步骤名称列独占宽度挤压请求列、
  ECU 信息区按钮独占一行压低字段区

### 变更
- **ECU 诊断子面板日志统一**: DTC/DID/IO/例程/会话/安全/序列 7 个
  面板的本地日志窗口移除，统一汇入底部业务日志
  （按级别着色、带 [功能页] 前缀，各面板释放 120~200px 高度）
- **UDS 服务发送区去重**: Request/Response/状态三行显示并入
  Timing 历史表（一行一条），输入预览改为 tooltip 悬浮
- 全车扫描/清除 DTC 提示词泛化（不再假设仅 CAN 连接）

## [1.4.0] - 2026-08-31

### 新增
- **可执行文件打包**（PyInstaller onedir）: 免安装Python直接分发使用
  - `python scripts/build_exe.py --clean` 一键构建，产物在 dist/DiagTools/，
    整个文件夹压缩即可发给他人
  - 应用图标、windowed无控制台、排除未用Qt组件（发行体积约120MB）
  - resources/、plugins/、bridge_worker.py 以文件形态置于exe旁：
    配置可编辑、插件可替换、32位桥接脚本可被外部Python直接执行
  - 运行时 logs/、data_recordings/、reports/ 生成在exe旁边，
    不再写入解包临时目录
- 新增 `src/utils/paths.py` 冻结感知的统一路径解析

### 修复
- 主窗口 `_project_root` 回溯层数错误：报告目录/ECU定义目录/
  A2L、ODX导入对话框曾指向 src/ 而非项目根（预置bug，已随路径统一修复）
- 安全算法插件目录改为锚定项目根（双击启动时不再依赖CWD）

## [1.3.0] - 2026-08-31

### 新增
- Trace标准格式导出（发给他人在CANoe/Wireshark中分析）:
  - **ASC** (Vector ASCII) / **BLF** (Vector二进制) — CAN报文，
    经python-can写入，CANoe原生可读；BLF导出后回读验证
  - **pcap** — DoIP/以太网报文，手工重建完整报文链
    (Ethernet+IPv4+UDP 13400+DoIP诊断消息，含正确IPv4校验和)，
    Wireshark原生可读
  - 导出入口: 报文分析/底部日志面板的"导出"按钮，
    按扩展名自动选择格式，DoIP逻辑地址自动携带
- 连接面板命名修正: "CAN接口配置"→"通信接口配置"（下拉含DoIP）
- DoIP"本地虚拟ECU（回环模拟）"勾选项: 连接时自动在本机
  TCP端口启动虚拟ECU，无硬件即可本地测试；断开自动回收

### 变更
- 导出完成/失败增加结果提示弹窗

## [1.2.0] - 2026-08-31

### 新增
- DoIP 诊断支持（ISO 13400-2）：TCP 路由激活 + 诊断消息收发，
  UDS 客户端可无缝切换 CAN/以太网传输
- 连接面板新增 DoIP 接口类型（IP/端口/Tester 与 ECU 逻辑地址），
  支持 UDP 广播「发现ECU」自动填入地址
- 虚拟 DoIP ECU（回环模拟，复用 VirtualEcuSimulator），
  无硬件即可体验完整 DoIP 诊断链路
- 日志路径显眼化：日志面板头部路径标签（跟随页签）点击打开对应目录、
  打开日志文件夹按钮、状态栏 Logging 块可点击
- DoIP 单元测试 11 项（回环，无硬件依赖）

### 变更
- `UdsClient` 支持注入自定义传输层（`transport_layer` 参数）

## [1.1.0] - 2026-08-31

### 新增
- 特殊功能页：ECU Definition `special_functions` 配置驱动
  （routine/did/reset/raw 四类动作，后台线程执行，正/负响应判定）
- 全局异常防护：未捕获异常记日志并弹窗，不再静默闪退；
  Qt 消息接入日志系统
- 版本号集中管理（`src.__version__`）
- GitHub Actions CI：双 Python 版本编译 + 单元测试

### 变更
- 删除最后一个占位视图（placeholder_view.py）
- 新增特殊功能单元测试（36 项全部通过）

## [1.0.0] - 2026-08-31

### 新增
- 完整 UDS 协议栈（ISO 14229-1 核心服务 + ISO 15765-2 传输层）
- CAN 硬件抽象层：Virtual（内置 ECU 仿真）/ PCAN（32 位桥接）/ Vector
- 8 个一级工作台：车辆总览、ECU 诊断、刷写中心、标定中心、测试中心、
  报文分析、报告中心、工具中心
- DID / DTC 管理器：JSON 定义、轮询、状态位可视化、时间线、CSV 导出
- 刷写中心：配置驱动 6 步流程，支持 bin/s19/s28/s37/hex 固件
- 安全算法插件：Python / DLL 动态加载（32 位桥接）
- 序列引擎、全车扫描、诊断报告生成（HTML/文本）
- 报文分析：CAN/UDS Trace、原始报文、报文重放、DBC 信号级解码
- 数据导入：DID/DTC 配置、Flash 配置、A2L、ODX/PDX/CDD、ECU Definition
- `.dproj` 项目文件：配置持久化、导入恢复、自动保存
- 分模块日志系统（通信/诊断/刷写/序列）
- 暗色 / 亮色主题
- unittest 单元测试套件
